import React, { memo } from 'react';
import CustomFieldsComponent from 'components/Settings/CustomFields/CustomFields';

const CustomFields = () => {
  return <CustomFieldsComponent />;
};

export default memo(CustomFields);
