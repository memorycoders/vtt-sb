import React, { Component } from 'react'
import { connect } from 'react-redux'

export const InfiniteRecruitmentListRow = () => {
  return (
    <div>

    </div>
  )
}

const mapStateToProps = (state) => ({

})

const mapDispatchToProps = {

}

export default connect(mapStateToProps, mapDispatchToProps)(InfiniteRecruitmentListRow)
