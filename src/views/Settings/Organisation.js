import React, { memo } from 'react';
import OrganisationComponent from 'components/Settings/Organisation/Organisation';

const Organisation = () => {
  return <OrganisationComponent />;
};

export default memo(Organisation);
